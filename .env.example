# Required for local run and Docker mount scope.
# Can be a palace directory or a direct chroma.sqlite3 file path.
MEMORY_PALACE_PATH=./palace
PALACE_PATH=./palace

# Local bind address for Docker published port.
BIND_HOST=127.0.0.1
PORT=8099
HOST=127.0.0.1
